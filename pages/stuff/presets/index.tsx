import { FC } from "react";
import Layout from "../../../components/shared/layout"

const Presets: FC = () => (
	<Layout>
		<h1 className="text-6xl font-bold text-white">
			Presets and Packs, the good kind!
		</h1>
	</Layout>
)

export default Presets
